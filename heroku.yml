# Heroku configuration for Docker deployment
build:
  docker:
    web: Dockerfile

# Release phase - run migrations/initialization
# release:
#   image: web
#   command:
#     - node apps/agor-daemon/dist/index.js --init

run:
  web: node apps/agor-daemon/dist/index.js

